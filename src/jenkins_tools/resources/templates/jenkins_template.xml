<?xml version='1.0' encoding='UTF-8'?>
<project>
  <actions/>
  <description>AUTOMATICALLY GENERATED JOB, DO NOT EDIT BY HAND! This job was generated by @(USERNAME) on @(HOSTNAME) at @(TIME) from the 'github.com:willowgarage/buildfarm' repository, and runs the @(SCRIPT) script on @(UBUNTU_DISTRO) / @(ARCH).</description>
  <logRotator>
    <daysToKeep>180</daysToKeep>
    <numToKeep>-1</numToKeep>
    <artifactDaysToKeep>-1</artifactDaysToKeep>
    <artifactNumToKeep>-1</artifactNumToKeep>
  </logRotator>
  <keepDependencies>false</keepDependencies>
  <properties>
    <hudson.plugins.trac.TracProjectProperty>
      <tracWebsite>http://code.ros.org/trac/ros/</tracWebsite>
    </hudson.plugins.trac.TracProjectProperty>
  </properties>
  @(VCS)
  <assignedNode>@(NODE)</assignedNode>
  <canRoam>false</canRoam>
  <disabled>false</disabled>
  <blockBuildWhenUpstreamBuilding>false</blockBuildWhenUpstreamBuilding>
  @(TRIGGER)
  <concurrentBuild>false</concurrentBuild>
  <builders>
    <hudson.tasks.Shell>
      <command>
echo &quot;_________________________________BEGIN SCRIPT______________________________________&quot;
sudo sh -c 'echo "deb http://packages.ros.org/ros/ubuntu @(UBUNTU_DISTRO) main" > /etc/apt/sources.list.d/ros-latest.list'
wget http://packages.ros.org/ros.key -O - | sudo apt-key add -
sudo apt-get update
sudo apt-get install python-jenkins-tools
bash run_chroot_local @(UBUNTU_DISTRO) @(ARCH) $WORKSPACE @(SCRIPT) @(SCRIPT_ARGS)
echo &quot;_________________________________END SCRIPT_______________________________________&quot;
     </command>
    </hudson.tasks.Shell>
  </builders>
  <publishers>
    <hudson.tasks.junit.JUnitResultArchiver>
      <testResults>test_results/*.xml</testResults>
      <keepLongStdio>false</keepLongStdio>
      <testDataPublishers/>
    </hudson.tasks.junit.JUnitResultArchiver>
    <hudson.tasks.Mailer>
      <recipients>@(EMAIL)</recipients>
      <dontNotifyEveryUnstableBuild>false</dontNotifyEveryUnstableBuild>
      <sendToIndividuals>false</sendToIndividuals>
    </hudson.tasks.Mailer>
  </publishers>
  <buildWrappers/>
</project>
